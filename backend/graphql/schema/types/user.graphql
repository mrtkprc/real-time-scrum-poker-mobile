type Query {
    user(id: ID!): User!
}

type User {
    id: ID!
    nickname: String!
    session_number: Int!
}

type Mutation{
    createUser(data: CreateUserInput!): User!
}

input CreateUserInput {
    nickname: String!,
    session_number: Int!
}
